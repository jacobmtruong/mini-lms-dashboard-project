import Logo from "../../components/shared/Logo";

function LoginPage() {
  return (
    <section className="login-placeholder">
      <div className="login-placeholder__content">
        <Logo />

        <h1>Welcome back</h1>

        <p>The complete login form will be added in the authentication task.</p>
      </div>
    </section>
  );
}

export default LoginPage;
